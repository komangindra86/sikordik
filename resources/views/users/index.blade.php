<x-layouts.app title="Manajemen pengguna">
    <div class="mb-6 flex flex-col justify-between gap-4 sm:flex-row sm:items-end"><div><p class="text-sm font-semibold text-brand-600">Akses sistem</p><h1 class="text-2xl font-bold text-slate-900">Manajemen pengguna</h1><p class="mt-1 text-sm text-slate-500">Kelola akun, multi-role, dan cakupan KSM.</p></div>@if(auth()->user()->hasPermission('users.create'))<a class="btn-primary" href="{{ route('users.create') }}">Tambah pengguna</a>@endif</div>
    <form class="card mb-4 flex gap-2" method="GET"><div class="flex-1"><label class="sr-only" for="q">Cari</label><input class="mt-0" id="q" name="q" value="{{ $search }}" placeholder="Cari nama atau email"></div><button class="btn-secondary" type="submit">Cari</button></form>
    <div class="space-y-3 md:hidden">@forelse($users as $user)<article class="card"><div class="flex items-start justify-between gap-3"><div><h2 class="font-bold text-slate-900">{{ $user->name }}</h2><p class="text-sm text-slate-500">{{ $user->email }}</p></div><span class="{{ $user->is_active ? 'badge-success' : 'badge-muted' }}">{{ $user->is_active ? 'Aktif' : 'Nonaktif' }}</span></div><p class="mt-3 text-xs text-slate-500">{{ ($rolesByUser[$user->id] ?? collect())->pluck('name')->join(', ') ?: 'Belum ada role' }}</p>@if(auth()->user()->hasPermission('users.update'))<a class="btn-secondary mt-4 w-full" href="{{ route('users.edit', $user->id) }}">Ubah pengguna</a>@endif</article>@empty<div class="card text-center text-sm text-slate-500">Belum ada pengguna.</div>@endforelse</div>
    <div class="card hidden overflow-x-auto p-0 md:block"><table class="w-full text-left text-sm"><thead class="border-b border-slate-200 bg-slate-50 text-xs uppercase text-slate-500"><tr><th class="px-5 py-3">Pengguna</th><th class="px-5 py-3">Role</th><th class="px-5 py-3">Status</th><th class="px-5 py-3 text-right">Aksi</th></tr></thead><tbody class="divide-y divide-slate-100">@forelse($users as $user)<tr><td class="px-5 py-4"><p class="font-semibold text-slate-900">{{ $user->name }}</p><p class="text-slate-500">{{ $user->email }}</p></td><td class="px-5 py-4 text-slate-600">{{ ($rolesByUser[$user->id] ?? collect())->pluck('name')->join(', ') ?: '—' }}</td><td class="px-5 py-4"><span class="{{ $user->is_active ? 'badge-success' : 'badge-muted' }}">{{ $user->is_active ? 'Aktif' : 'Nonaktif' }}</span></td><td class="px-5 py-4 text-right">@if(auth()->user()->hasPermission('users.update'))<a class="font-semibold text-brand-700 hover:underline" href="{{ route('users.edit', $user->id) }}">Ubah</a>@endif</td></tr>@empty<tr><td class="px-5 py-8 text-center text-slate-500" colspan="4">Belum ada pengguna.</td></tr>@endforelse</tbody></table></div>
    <div class="mt-4">{{ $users->links() }}</div>
</x-layouts.app>
