<x-layouts.app title="Notifikasi">
    <h1 class="mb-5 text-2xl font-bold">Notifikasi</h1><div class="space-y-3">@forelse($notifications as $n)<article class="card flex flex-wrap items-center justify-between gap-3 p-5"><div><p class="font-semibold">{{ $n->message }}</p><p class="text-sm text-slate-500">{{ \Carbon\Carbon::parse($n->created_at)->format('d-m-Y H:i') }} WITA · {{ $n->read_at ? 'Sudah dibaca' : 'Belum dibaca' }}</p></div>@if(!$n->read_at)<form method="POST" action="{{ route('scheduling.notification-read', $n->ulid) }}">@csrf<button class="btn-secondary">Tandai dibaca</button></form>@endif</article>@empty<div class="card p-5 text-slate-500">Belum ada notifikasi.</div>@endforelse</div><div class="mt-4">{{ $notifications->links() }}</div><a class="mt-5 inline-block text-brand-700" href="{{ route('scheduling.index') }}">Buka penugasan & jadwal →</a>
</x-layouts.app>
