<x-layouts.app title="Audit log">
    <div class="mb-6"><p class="text-sm font-semibold text-brand-600">Keamanan & keterlacakan</p><h1 class="text-2xl font-bold text-slate-900">Audit log</h1><p class="mt-1 text-sm text-slate-500">Catatan ini hanya-baca dan tidak menyediakan aksi hapus.</p></div>
    <form class="card mb-4 flex gap-2" method="GET"><div class="flex-1"><label class="sr-only" for="event">Filter kejadian</label><input class="mt-0" id="event" name="event" value="{{ $event }}" placeholder="Contoh: user.updated"></div><button class="btn-secondary" type="submit">Filter</button></form>
    <div class="space-y-3">@forelse($logs as $log)<article class="card"><div class="flex flex-col justify-between gap-2 sm:flex-row"><div><span class="badge-muted">{{ $log->event }}</span><h2 class="mt-2 font-semibold text-slate-900">{{ $log->description ?: 'Aktivitas sistem' }}</h2><p class="mt-1 text-sm text-slate-500">{{ $log->user_name ?: 'Sistem' }} · {{ \Carbon\Carbon::parse($log->created_at)->timezone(config('app.timezone'))->format('d-m-Y H:i:s') }}</p></div><p class="text-xs text-slate-400">{{ $log->ip_address ?: 'IP tidak tersedia' }}</p></div>@if($log->reason)<div class="mt-3 rounded-xl bg-amber-50 p-3 text-sm text-amber-900"><strong>Alasan:</strong> {{ $log->reason }}</div>@endif @if($log->old_values || $log->new_values)<details class="mt-3"><summary class="cursor-pointer text-sm font-semibold text-brand-700">Lihat perubahan</summary><div class="mt-2 grid gap-3 lg:grid-cols-2"><pre class="overflow-x-auto rounded-xl bg-slate-900 p-3 text-xs text-slate-100">Sebelum:\n{{ json_encode(json_decode($log->old_values ?: '{}'), JSON_PRETTY_PRINT | JSON_UNESCAPED_UNICODE) }}</pre><pre class="overflow-x-auto rounded-xl bg-slate-900 p-3 text-xs text-slate-100">Sesudah:\n{{ json_encode(json_decode($log->new_values ?: '{}'), JSON_PRETTY_PRINT | JSON_UNESCAPED_UNICODE) }}</pre></div></details>@endif</article>@empty<div class="card text-center text-sm text-slate-500">Belum ada aktivitas tercatat.</div>@endforelse</div><div class="mt-4">{{ $logs->links() }}</div>
</x-layouts.app>
