<!doctype html><html lang="id"><head><meta charset="utf-8"><style>body{font-family:DejaVu Sans,sans-serif;font-size:8px;color:#17233a}h1{font-size:16px}table{width:100%;border-collapse:collapse;table-layout:fixed}td,th{border:1px solid #cbd5e1;padding:5px;overflow-wrap:break-word}th{background:#e2e8f0}thead{display:table-header-group}tr{page-break-inside:avoid}</style></head><body><h1>SIKORDIK — {{ $title }}</h1><p>Dibuat {{ now()->format('d-m-Y H:i') }} · Salinan laporan sesuai hak akses pembuat. Bukan dokumen pengesahan.</p><table>@if($rows)<thead><tr>@foreach(array_keys($rows[0]) as $key)<th>{{ $key }}</th>@endforeach</tr></thead><tbody>@foreach($rows as $row)<tr>@foreach($row as $value)<td>{{ $value ?? '—' }}</td>@endforeach</tr>@endforeach</tbody>@else<tr><td>Tidak ada data.</td></tr>@endif</table></body></html>
