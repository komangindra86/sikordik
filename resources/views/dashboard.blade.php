<x-layouts.app title="Dashboard">
<div class="mb-6"><p class="text-sm font-semibold text-brand-600">Pendidikan klinis RSBM</p><h1 class="text-2xl font-bold">Dashboard</h1><p class="mt-2 text-sm text-slate-600">Ringkasan sesuai peran dan cakupan Anda. Klik kartu untuk membuka modul terkait.</p></div>
<section class="grid gap-4 sm:grid-cols-2 xl:grid-cols-4">@foreach($cards as $card)<a href="{{ $card['url'] }}" class="card"><p class="text-sm text-slate-600">{{ $card['label'] }}</p><p class="mt-2 text-3xl font-bold text-brand-700">{{ number_format($card['value']) }}</p></a>@endforeach</section>
<div class="mt-6 grid gap-5 xl:grid-cols-2"><section class="card"><h2 class="text-lg font-bold">Jadwal hari ini</h2><p class="text-xs text-slate-500">Maksimal 20 jadwal terbit dalam cakupan Anda.</p><ul class="mt-3 space-y-3">@forelse($today as $s)<li class="border-t pt-2"><p>{{ $s->activity }}</p><p class="text-xs text-slate-500">{{ $s->date }} · {{ $s->status }}</p></li>@empty<li>Belum ada jadwal terbit hari ini.</li>@endforelse</ul><a class="mt-3 inline-block underline" href="{{ route('scheduling.index') }}">Buka jadwal dan penugasan</a></section>
<section class="card overflow-x-auto"><h2 class="text-lg font-bold">Peserta aktif menurut institusi dan KSM</h2><p class="text-xs text-slate-500">Maksimal 50 kelompok; laporan menyediakan daftar lengkap.</p><table class="mt-3 w-full text-sm"><thead><tr><th class="p-2 text-left">Institusi</th><th class="p-2 text-left">KSM</th><th class="p-2">Jumlah</th></tr></thead><tbody>@forelse($groups as $g)<tr class="border-t"><td class="p-2">{{ $g->institution }}</td><td class="p-2">{{ $g->department }}</td><td class="p-2 text-center">{{ $g->total }}</td></tr>@empty<tr><td colspan="3" class="p-2">Belum ada peserta aktif.</td></tr>@endforelse</tbody></table></section></div>
</x-layouts.app>
