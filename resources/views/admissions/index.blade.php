<x-layouts.app title="Penerimaan & penempatan">
    @include('admissions.nav')
    <div class="mb-5 flex flex-wrap items-center justify-between gap-3"><div><h1 class="text-2xl font-bold">Penerimaan & penempatan</h1><p class="mt-1 text-sm text-slate-500">Pantau pengajuan dan riwayat pendidikan dalam cakupan Anda.</p></div>@if($access->admin(auth()->user()))<a class="btn-primary" href="{{ route('admissions.create') }}">Tambah penempatan</a>@endif</div>
    <form class="card mb-5 flex flex-wrap items-end gap-3" method="GET"><label class="flex-1">Nama atau nomor peserta<input name="q" value="{{ request('q') }}" placeholder="Cari peserta"></label><label>Status<select name="status"><option value="">Semua status</option>@foreach(array_merge(['draft','ditolak_ksm','ditolak_kordik','dibatalkan'], \App\Services\PlacementService::RESERVING) as $s)<option @selected(request('status') === $s)>{{ $s }}</option>@endforeach</select></label><button class="btn-primary">Cari</button></form>
    <div class="card overflow-x-auto"><table class="w-full text-left text-sm"><thead><tr class="border-b"><th class="p-3">Peserta</th><th class="p-3">KSM / program</th><th class="p-3">Periode</th><th class="p-3">Status</th></tr></thead><tbody>@forelse($placements as $p)@php($snapshot = json_decode($p->snapshot, true))<tr class="border-b border-slate-100"><td class="p-3"><a class="font-semibold text-brand-700 underline" href="{{ route('admissions.show', $p->ulid) }}">{{ $participants[$p->participant_id]->name }}</a><p class="text-xs text-slate-500">{{ $participants[$p->participant_id]->number }}</p></td><td class="p-3">{{ $snapshot['department'] }}<p class="text-xs">{{ $snapshot['program'] }}</p></td><td class="p-3">{{ $p->start_date }} — {{ $p->end_date }}</td><td class="p-3"><span class="badge-muted">{{ $p->status }}</span></td></tr>@empty<tr><td colspan="4" class="p-8 text-center text-slate-500">Belum ada penempatan dalam cakupan ini.</td></tr>@endforelse</tbody></table><div class="mt-4">{{ $placements->links() }}</div></div>
</x-layouts.app>
