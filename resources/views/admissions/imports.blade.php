<x-layouts.app title="Impor peserta">
    @include('admissions.nav')<h1 class="mb-4 text-2xl font-bold">Impor peserta</h1><div class="card mb-5"><p class="text-sm">Gunakan satu worksheet XLSX tanpa macro, maksimum 5 MB / 500 peserta. Kolom A–E pada baris pertama: <strong>name, birth_date, nik, nim, email</strong>. Format seluruh kolom sebagai teks; tanggal YYYY-MM-DD. Formula dan tautan eksternal ditolak.</p><p class="mt-3 text-sm">Hasil diperiksa per baris sebelum konfirmasi. Peserta lama dipilih eksplisit; tidak ada penggabungan otomatis.</p></div>
    <form class="card mb-5 space-y-4" method="POST" enctype="multipart/form-data" action="{{ route('admissions.import-preview') }}">@csrf<label class="block">Institusi<select name="institution_id" required><option value="">Pilih</option>@foreach($institutions as $i)<option value="{{ $i->id }}">{{ $i->name }}</option>@endforeach</select></label><label class="block">Sumber XLSX<input name="file" type="file" accept=".xlsx" required></label><button class="btn-primary">Unggah dan pratinjau</button></form>
    <div class="card"><h2 class="mb-3 font-bold">Impor Anda</h2>@forelse($imports as $import)<p class="border-t py-3 text-sm"><a class="text-brand-700 underline" href="{{ route('admissions.import', $import->ulid) }}">{{ $import->created_at }} · {{ $import->ulid }}</a></p>@empty<p class="text-sm text-slate-500">Belum ada impor.</p>@endforelse{{ $imports->links() }}</div>
</x-layouts.app>
