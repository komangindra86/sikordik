<x-layouts.app :title="$definition['label']">
    <div class="mb-6 flex flex-col justify-between gap-4 sm:flex-row sm:items-end"><div><p class="text-sm font-semibold text-brand-600">Data master</p><h1 class="text-2xl font-bold text-slate-900">{{ $definition['label'] }}</h1><p class="mt-1 text-sm text-slate-500">Kelola data referensi yang dipakai sistem.</p></div>@if(auth()->user()->hasPermission('masters.create'))<a class="btn-primary" href="{{ route('masters.create', $master) }}">Tambah data</a>@endif</div>
    <form class="card mb-4 flex gap-2" method="GET"><div class="flex-1"><label class="sr-only" for="q">Cari</label><input class="mt-0" id="q" name="q" value="{{ $search }}" placeholder="Cari nama"></div><button class="btn-secondary" type="submit">Cari</button></form>
    <div class="space-y-3 md:hidden">@forelse($records as $record)<article class="card"><div class="flex items-start justify-between gap-3"><div><h2 class="font-bold text-slate-900">{{ $record->name }}</h2>@if(isset($record->code))<p class="text-sm text-slate-500">{{ $record->code }}</p>@endif</div><span class="{{ $record->is_active ? 'badge-success' : 'badge-muted' }}">{{ $record->is_active ? 'Aktif' : 'Nonaktif' }}</span></div>@if(auth()->user()->hasPermission('masters.update'))<a class="btn-secondary mt-4 w-full" href="{{ route('masters.edit', [$master, $record->id]) }}">Ubah data</a>@endif</article>@empty<div class="card text-center text-sm text-slate-500">Belum ada data.</div>@endforelse</div>
    <div class="card hidden overflow-x-auto p-0 md:block"><table class="w-full text-left text-sm"><thead class="border-b border-slate-200 bg-slate-50 text-xs uppercase text-slate-500"><tr><th class="px-5 py-3">Nama</th><th class="px-5 py-3">Kode/Identitas</th><th class="px-5 py-3">Status</th><th class="px-5 py-3 text-right">Aksi</th></tr></thead><tbody class="divide-y divide-slate-100">@forelse($records as $record)<tr><td class="px-5 py-4 font-semibold text-slate-900">{{ $record->name }}</td><td class="px-5 py-4 text-slate-500">{{ $record->code ?? $record->employee_number ?? '—' }}</td><td class="px-5 py-4"><span class="{{ $record->is_active ? 'badge-success' : 'badge-muted' }}">{{ $record->is_active ? 'Aktif' : 'Nonaktif' }}</span></td><td class="px-5 py-4 text-right">@if(auth()->user()->hasPermission('masters.update'))<a class="font-semibold text-brand-700 hover:underline" href="{{ route('masters.edit', [$master, $record->id]) }}">Ubah</a>@endif</td></tr>@empty<tr><td class="px-5 py-8 text-center text-slate-500" colspan="4">Belum ada data.</td></tr>@endforelse</tbody></table></div>
    <div class="mt-4">{{ $records->links() }}</div>
</x-layouts.app>
